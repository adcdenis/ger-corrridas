# Configurações do Servidor
PORT=5000
NODE_ENV=development

# Banco de Dados MongoDB
MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/database?retryWrites=true&w=majority

# JWT Secret (use um valor seguro em produção)
JWT_SECRET=seu_jwt_secret_super_seguro_aqui

# Frontend URL (para CORS em produção)
FRONTEND_URL=https://seu-frontend.vercel.app

# Vercel (definido automaticamente no deploy)
# VERCEL=1